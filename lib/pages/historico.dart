import 'package:flutter/material.dart';
 
class RutasHistoricas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Container(
            child: Text('Rutas Historicas'),
          ),
        ),
    );
  }
}