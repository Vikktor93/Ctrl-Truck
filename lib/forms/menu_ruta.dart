import 'package:flutter/material.dart';
 
class MenuRuta extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Container(
            child: Text('Menu Ruta'),
          ),
        ),
      );
  }
}